# Semantic single linkage

The implementation uses the paper's weighted positional and clipped-cosine
distance, global single-linkage merges, a maximum cluster sentence count, and a
stop threshold. Clusters can be non-contiguous and retain multiple source spans.
The historical method name is not a verified paper-exact claim.
